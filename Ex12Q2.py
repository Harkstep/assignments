tup = 'Hello'                          # This is a string disguised by using variable tup
print(type(tup))                       # This print command types the class to show it's a string
print(len(tup))                        # As this is a string the len counts each character in the string.


tup = 'Hello',                         # This comma turns the string into a Tuple, Although it is best
print(type(tup))                       # practice to use () to show it's a Tuple the brackets are not
print(len(tup))                        # actually necessary. A tuple is a collection of objects so as
                                       # there is only one object, the return length is one.

